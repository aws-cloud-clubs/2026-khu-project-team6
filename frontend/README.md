
  # Event-based Rental App

  This is a code bundle for Event-based Rental App. The original project is available at https://www.figma.com/design/XZZ5JBonRObl7r42DOrGc4/Event-based-Rental-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  